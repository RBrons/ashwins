- if request.xhr?
  == render partial: "entities/llp/basic_info"
- else
  .corporate-forms[style=(@entity.persisted? ? '' : 'margin-top: 0')]
    == render partial: "entities/llp/links"
    .corporate-contact-form
      == render partial: "entities/llp/basic_info"
