- if request.xhr?
  == render partial: "entities/guardianship/basic_info"
- else
  .corporate-forms[style=(@entity.persisted? ? '' : 'margin-top: 0')]
    == render partial: "entities/guardianship/links"
    .clearfix
    .corporate-contact-form
      == render partial: "entities/guardianship/basic_info"
